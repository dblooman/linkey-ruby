#!/usr/bin/env ruby
require 'rubygems'
require 'bundler/setup'

begin
  require 'linky/cli'
  Linky::CLI.start
rescue Interrupt => e
  puts "\nQuitting..."
  exit 1
rescue SystemExit => e
  exit e.status
end
